/**
 * Lite stub for `@/components/localsend/LocalSendManager`.
 * LocalSend (LAN book transfer) is a Tauri-native-only feature; Lite is web-only.
 * The stub renders nothing so the ReaderContent tree stays intact.
 */

import React from 'react';

const LocalSendManager: React.FC = () => null;

export default LocalSendManager;
