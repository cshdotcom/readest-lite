// Lite stub — S3 sync provider not available
import type { S3Settings } from '@/types/settings';
export const buildS3Provider = (): null => null;
export const createS3Provider = (_settings: S3Settings): null => null;
